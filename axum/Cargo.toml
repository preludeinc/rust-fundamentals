[package]
name = "axum"
version = "0.1.0"
edition = "2021"

[dependencies]
# serde / json
serde = { version = "1", features = ["derive"]}
tokio = { version = "1", features = ["full"]}
axum = "0.8"
serde_json = "1.0"

[dev-dependencies]
anyhow = "1.0"
httpc-test = "0.1.1"